/** Owner: Abhi. Placeholder until Krrish's /inspect + sample images are ready. */
export default function Vision() {
  return (
    <main>
      <h1 className="font-display text-2xl mb-6 text-gray-100">AI Vision Inspection</h1>
      <div className="bg-panel border border-line rounded-lg p-4 text-gray-400 font-mono text-sm">
        TODO(Abhi): once Krrish's /inspect endpoint + sample_images exist,
        show a looping image feed with bounding-box overlays and a running
        defect log here.
      </div>
    </main>
  );
}
